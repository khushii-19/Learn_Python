# from typing import List, Union, Tuple 
#by exporting it we can use it directly by passing list like n : list = 

n : int = 5

name: str = "Harry"


def  sum(a: int, b: int) -> int:
    return a+b

a = sum(3,4)
print(a)